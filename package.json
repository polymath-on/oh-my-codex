{
  "name": "oh-my-codex",
  "version": "0.8.11",
  "description": "Multi-agent orchestration layer for OpenAI Codex CLI",
  "type": "module",
  "bin": {
    "omx": "bin/omx"
  },
  "scripts": {
    "build": "cargo build",
    "check:no-unused": "tsc -p tsconfig.no-unused.json",
    "dev": "tsc --watch",
    "lint": "biome lint .",
    "setup": "cargo run -p omx-cli -- setup",
    "doctor": "cargo run -p omx-cli -- doctor",
    "ask:claude": "./scripts/ask-claude.sh",
    "ask:gemini": "./scripts/ask-gemini.sh",
    "test:node": "npm run build:js && node --test $(find dist -name '*.test.js')",
    "test": "npm run build:js && npm run test:node && node scripts/generate-catalog-docs.js --check",
    "coverage:team-critical": "npm run build:js && c8 --all --src dist/team --src dist/state --include 'dist/team/**/*.js' --include 'dist/state/**/*.js' --exclude '**/__tests__/**' --reporter=text-summary --reporter=lcov --reporter=json-summary --report-dir coverage/team --check-coverage --lines=78 --functions=90 --branches=70 --statements=78 node --test $(find dist/team/__tests__ dist/state/__tests__ -name '*.test.js')",
    "test:compat:node": "npm run build:js && node --test dist/compat/__tests__/*.test.js",
    "test:compat:rust": "cargo build && OMX_COMPAT_TARGET=./target/debug/omx npm run test:compat:node",
    "build:js": "tsc"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin/",
    "agents/",
    "skills/",
    "prompts/",
    "templates/",
    "scripts/",
    "release/",
    "native/"
  ],
  "keywords": [
    "codex",
    "openai",
    "cli",
    "agents",
    "orchestration",
    "multi-agent"
  ],
  "author": "Yeachan Heo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yeachan-Heo/oh-my-codex.git"
  },
  "homepage": "https://yeachan-heo.github.io/oh-my-codex",
  "bugs": {
    "url": "https://github.com/Yeachan-Heo/oh-my-codex/issues"
  },
  "license": "MIT",
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/node": "^22.19.11",
    "c8": "^10.1.3",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "ajv": ">=8.18.0",
    "hono": ">=4.11.10"
  }
}
